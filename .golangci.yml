version: "2"
linters:
  enable:
    - durationcheck
    - forcetypeassert
    - godot
    - makezero
    - misspell
    - nilerr
    - predeclared
    - unconvert
    - unparam
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gofmt
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
